# Dependencies
node_modules/

# Build output
dist/
build/
out/
*.tsbuildinfo

# Tests / coverage
coverage/
.vite/
.cache/
*.local
.env
.env.*
!.env.example

# Internal design docs (not part of the public release)
uploads/
FIX_SUMMARY_*.md
BATCH_*_COMPLETE.md
EAOIN/docs/
EAOIN/BUGFIX_*.md
EAOIN/BATCH_*.md
EAOIN/RELEASE_*.md
EAOIN/NEXT_GENERATION_*.md

# Compiled bundles
EAOIN/*.zip
EAOIN/*.tar.gz

# Desktop packaging (Electron) — commit the icon build resources, but ignore the
# staged game build, produced installers, and desktop-only node_modules.
!EAOIN/desktop/build/
EAOIN/desktop/build/*
!EAOIN/desktop/build/icon.png
EAOIN/desktop/game-build/
EAOIN/desktop/release/
EAOIN/desktop/node_modules/

