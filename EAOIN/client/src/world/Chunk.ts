// Compatibility bridge for legacy client systems that expect Chunk under client/src/world.
export { Chunk, CHUNK_HEIGHT, CHUNK_SIZE } from '../../../src/world/Chunk';
export type { BlockID } from '@shared/blocks/BlockRegistry';
